[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-ai"
version = "0.1.0"
description = "This repository contains the codebase developed while learning/teaching Agentic AI to friends and colleagues"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
license = { file = "LICENSE" }
